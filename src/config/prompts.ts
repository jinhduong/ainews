export const OPENAI_NEWS_PROMPT = (category: string) => `Provide 5 recent news headlines and a very brief summary (1-2 sentences) for each, related to the category: ${category}. Also include a plausible URL and a recent publication date in YYYY-MM-DD format. Format the output as a JSON array of objects, where each object has 'id', 'title', 'summary', 'url', and 'publishedAt' fields. Ensure the URLs are unique and look realistic.`;

export const GEMINI_NEWS_PROMPT = (category: string) => `Provide 5 recent news headlines and a very brief summary (1-2 sentences) for each, related to the category: ${category}. Also include a plausible URL and a recent publication date in YYYY-MM-DD format. Format the output as a JSON array of objects, where each object has 'id', 'title', 'summary', 'url', and 'publishedAt' fields. Ensure the URLs are unique and look realistic.`;